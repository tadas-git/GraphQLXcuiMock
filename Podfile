# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TopQuestion' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TopQuestion
  pod "Apollo", '~> 0.27.0'

end

target 'XCUITests' do
    inherit! :search_paths
    pod 'Swifter', '~> 1.4.7'
 end
